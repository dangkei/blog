<%- include header %>
<% include paging %>
<br/>
<% posts.forEach(function (post, index) { %>
  <p>
  <h6><a href="/u/<%= post.name %>/<%= post.time.day %>/<%= post.title %>"><%= post.title %></a>  ：<%= post.time.minute %></h6>
  </p>
  <p><%- post.post %></p>
<% }) %>
<%- include footer %>