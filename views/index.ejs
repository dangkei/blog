
<%- include header.ejs %>
<%- include paging %><br>
<% posts.forEach(function (post, index) { %>
  <p><h5><a href="u/<%= post.name %>/<%= post.time.day %>/<%= post.Guid %>"><%= post.title %></a></h5></p>
  <p class="info">
    作者：<a href="u/<%= post.name %>"><%= post.name %></a> |
    日期：<%= post.time.minute %>
  </p>
  <div class="well jumbotron">
  <p><%- post.post %></p>
  </div>
  <p class="info">阅读：<%= post.pv %> | 评论：<%= post.comments.length %></p>
  <% }) %>



</div>
<%- include footer %>